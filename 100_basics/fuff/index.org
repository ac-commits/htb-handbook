#+title: Index
* Background
=ffuf= *Fuzz Faster U Fool* written in =Go=.


For fuzzing a subdomain we encouter sometimes a subdomain and add this to our hosts file.

* Examples
#+begin_src bash
fuff -w /usr/share/wordlists/seclists/Discovery/DNS/combined_subdomains.txt -u http://$URL/ -H 'Host: FUZZ.$URL' -mc

#+end_src
